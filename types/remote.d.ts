/**
 * Types for `nisaba/remote` (src/db-remote.js) -- the WASM-free
 * main-thread entry: the pure-JS codec plus the Worker RPC bridge.
 */
export { encode, decode, ObjectId, Pointer, createRemoteBridge, RemoteBridge } from './nisaba.js';
